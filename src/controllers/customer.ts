import { Customer } from '../models/customer';

export const setCustomers = async (req: any, res: any) => {
  // res.send('set Customers');
};

export const getAllCustomers = (req: any, res: any) => {
  // res.send('get all customers');
};

export const setAdmin = (req: any, res: any) => {};
